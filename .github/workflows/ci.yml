name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder validation
        run: echo "Repository foundation workflow initialized. Add linting and test steps in later milestones."
